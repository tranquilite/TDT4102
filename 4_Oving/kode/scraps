
    while(running) {
        int q;
        for(int i=0; i < SIZE; ++i) {
            char gInn;
            cin >> gInn;
            guess[i] = toupper(gInn);
        }
        for(int i=0; i < SIZE; ++i) {
            if(code[i] != guess[i]) {
                break;
            } else {
                cout << "Gratulerer, du har klart det?\n";
            }
        }
